<% include ../partials/header %>
  <header class="jumbotron">
    <h1><span class="glyphicon glyphicon-tent align-bottom" aria-hidden="true"></span> Mirësevini në OffRoad!</h1>
    <p>Shikoni kampet tona të perzgjedhura nëpër Shqipëri</p>
    <form class="form-inline" action="/campgrounds" method="GET">
      <div class="form-group has-feedback">
        <label for="search"></label>
        <input class="form-control" type="text" name="search" placeholder="Kerkoni" autocomplete="off" />
        <span class="glyphicon glyphicon-search form-control-feedback" aria-hidden="true"></span>
      </div>
    </form>
    <hr>
    <a class="pull-right" href="/campgrounds/new">
      <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
      Ndani nje vendodhje te re
    </a>
  </header>

  <div class="row text-center" style="display: flex; flex-wrap: wrap;">
    <% if (noMatch !== null) {%>
      <div class="col-md-12">
        <h4><%= noMatch %></h4>
      </div>
    <% } %>
    <% campgrounds.forEach(campground => {%>
      <div class="col-md-3 col-sm-6">
        <div class="thumbnail card">
          <img class="campground__image" data-src="<%= campground.image.url %>" alt="<%= campground.name %>" />
          <div class="caption">
            <h4 class="caption"><%= campground.name %></h4>
          </div>
          <p>
            <a class="btn btn-primary btn-sm btn-b-mg" href="/campgrounds/<%= campground._id %>">
          Me shume informacion
            </a>
          </p>
        </div>
      </div>
    <% })%>
  </div>
<% include ../partials/footer %>
