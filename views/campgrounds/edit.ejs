<% include ../partials/header %>
<div class="row">
  <h1 style="text-align: center;">Ndryshoni <%= campground.name %></h1>
  <div class="rwd-form">
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="name">Emri</label>
        <input class="form-control" type="text" name="campground[name]" value="<%= campground.name %>" autocomplete="off" />
      </div>
      <div class="form-group">
        <label for="image">Përditëso imazhin</label>
        <input id="image" type="file" name="image" accept="image/*" autocomplete="off" />
      </div>
      <div class="form-group">
        <label for="price">Çmimi</label>
        <input class="form-control" type="number" name="campground[price]" value="<%= campground.price %>" min="0.01" step="0.01" autocomplete="off" />
      </div>
      <div class="form-group">
        <label for="location">Vendndodhja</label>
        <input class="form-control" type="text" name="campground[location]" id="location" value="<%= campground.location %>" autocomplete="off" />
      </div>
      <div class="form-group">
        <label for="description">Përshkrim</label>
        <input class="form-control" type="text" name="campground[description]" value="<%= campground.description %>" autocomplete="off" />
      </div>
      <div class="form-group">
        <button class="btn btn-md btn-primary btn-block">Perditsoni</button>
      </div>
    </form>
    <a href="/campgrounds/<%= campground._id %>">Mbrapa</a>
  </div>
</div>
<% include ../partials/footer %>